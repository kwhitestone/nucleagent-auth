# nucleagent-auth web（子应用）本地开发环境变量
# 复制为 .env.local 后按需修改。

# dev server 端口（默认 26678；micro-app 壳应用编排时可注入）
AUTH_WEB_PORT=26678

# /api 代理目标（auth 后端，默认 http://localhost:26670）
AUTH_BACKEND_URL=http://localhost:26670
